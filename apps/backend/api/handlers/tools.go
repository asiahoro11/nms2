package handlers

// Legacy network-tools handler implementations were retired after the module
// extraction. The active HTTP entrypoints now live in tools_module.go and
// delegate to apps/backend/modules/tools while keeping routes and payloads
// stable.
