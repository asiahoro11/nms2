// Made by YTSworks
// YTS工作室製作
package handlers

// Legacy report handler implementations were retired after the module
// extraction. The active HTTP entrypoints now live in reports_module.go and
// delegate to apps/backend/modules/reports while keeping routes and payloads
// stable.
