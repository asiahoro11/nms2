package handlers

// Legacy dashboard summary handler implementations were retired after the
// module extraction. The active HTTP entrypoints now live in
// dashboard_module.go and delegate to apps/backend/modules/dashboard while
// keeping routes and payloads stable.
